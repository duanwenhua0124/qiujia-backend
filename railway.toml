[build]
builder = "nixpacks"
buildCommand = "apt-get update && apt-get install -y python3 && npm ci"

[nixpacks]
PYTHON_VERSION = "3.11"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
